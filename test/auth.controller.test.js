const assets = require('assets').strict;
const authController = require('../src/controllers/auth');

describe('Auth controller', function () {
    describe('Login', function () {
        it('Has validation on two ')
    });
})